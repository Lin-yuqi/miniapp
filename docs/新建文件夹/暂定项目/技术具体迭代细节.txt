1.0 : 确定使用 crewai 框架，简单的接入 deepseek api v3版本 线性处理每个agent
1.1 :  更改业务流程，划分为 用户交流->需求分析->架构设计->生成代码
1.2 : 优化了 prompt
1.3 ： 对流程做了细化，将架构设计细分为架构设计和工程目录规划两个环节
1.4 : 发现 deepseek v3.2 版本上下文限制、输出限制较大。针对上下文限制引入了上下文压缩机制：只提示最相关的几个文件内容，按照重要性排序，并对内容做了截断；针对输出限制引入了分块编码机制：将任务分为多个模块，分批输出，并由机械化脚本完成实际的项目落地
2.0 : 将压缩算法由普通提示词改为单独的 task ，并引入新的 agent 负责，优化压缩效果，淘汰了简单的文本截断
2.1 ： 在压缩效果和实际生成精度间做了取舍，尝试了低中高三种压缩档位
2.2 : 引入上下文整理机制
3.0 : 设计了简单的页面 ui ，提供了友好的交互界面
3.1 : 改用了 deepseek v4 ，获得了更大的上下文长度和输出长度，将单文件生成改为了分页面生成+分组件生成
3.2 : 在流程中加入了一致性校验和中间产物留存
3.3 : 优化了 ui 显示，使生成逻辑更加透明
