[project]
name = "crewai-mini-program-generator"
version = "0.1.0"
description = "CrewAI and DeepSeek based WeChat mini program generator with a local chat-style UI."
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
  "crewai[tools]",
  "langchain",
  "openai",
]

[tool.uv]
package = false
